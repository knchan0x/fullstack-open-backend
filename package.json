{
  "name": "fullstack-open-backend",
  "version": "0.0.1",
  "description": "backend part of fullstack-open",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../fullstack-open/part2/phonebook/ && npm run build && cp -r ./dist ../../../fullstack-open-backend/",
    "build": "npm run build:ui",
    "lint": "eslint ."
  },
  "author": "Michael Chan",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.6.1",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "eslint": "^8.51.0",
    "nodemon": "^3.0.1"
  }
}
